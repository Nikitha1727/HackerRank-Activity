#include <stdio.h>

int main() {
    int n, i;

    // Read input
    scanf("%d", &n);
    scanf("%d", &i);

    // Toggle the i-th bit
    n = n ^ (1 << i);

    // Print result
    printf("%d\n", n);

    return 0;
}
