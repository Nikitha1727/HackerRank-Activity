#include <stdio.h>

int main()
{
    int n, x;

    scanf("%d %d", &n, &x);

    for (int i = 1; i <= n; i++)
    {
        printf("%d", i * x);

        if (i < n)
            printf(" ");
    }

    return 0;
}
