#include <stdio.h>

int main() {
    long long a, b;
    scanf("%lld %lld", &a, &b);

    int lastA = a - (a / 10) * 10;
    int lastB = b - (b / 10) * 10;

    if (lastA == lastB)
        printf("YES");
    else
        printf("NO");

    return 0;
}
